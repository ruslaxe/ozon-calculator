# 📤 Подробная инструкция: Как опубликовать проект на GitHub

Это пошаговое руководство для новичков. Следуйте инструкциям по порядку.

## 📋 Что нужно перед началом

1. **Аккаунт на GitHub** (если нет - создайте на https://github.com)
2. **Git установлен на компьютере** (проверьте командой `git --version`)

## 🚀 Шаг 1: Проверка установки Git

Откройте терминал (на Mac это Terminal, на Windows - Command Prompt или PowerShell) и выполните:

```bash
git --version
```

Если Git не установлен:
- **Mac**: 
  - Выполните: `xcode-select --install`
  - Появится диалоговое окно - нажмите **"Установить"**
  - Дождитесь завершения установки (5-15 минут)
  - После установки перезапустите терминал и проверьте: `git --version`
- **Windows**: Скачайте с https://git-scm.com/download/win

**Если появилась ошибка о лицензии Xcode:**
- Выполните в терминале: `sudo xcodebuild -license`
- Нажмите пробел, чтобы прокрутить лицензию вниз
- В конце введите `agree` и нажмите Enter
- Введите пароль администратора, если попросят

## 🔍 Шаг 2: Проверка текущего состояния проекта

Откройте терминал в папке вашего проекта и проверьте, инициализирован ли Git:

```bash
cd "/Users/ruslanilin/Desktop/cursor-prod/ecom-economy/project file"
git status
```

### Вариант A: Git уже инициализирован

Если вы видите список файлов - переходите к Шагу 3.

### Вариант B: Git не инициализирован

Если видите ошибку "not a git repository", выполните:

```bash
git init
```

## 🔒 Шаг 3: Настройка Git (если еще не настроен)

Выполните эти команды один раз (замените на свои данные):

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

**Важно**: Используйте email, привязанный к вашему GitHub аккаунту!

## 📝 Шаг 4: Проверка и подготовка файлов

### 4.1. Убедитесь, что важные файлы на месте:

```bash
ls -la | grep -E "(README|LICENSE|CONTRIBUTING|env.example)"
```

Должны быть видны:
- README.md
- LICENSE
- CONTRIBUTING.md
- env.example

### 4.2. Проверьте .gitignore

Убедитесь, что `.gitignore` содержит строки для исключения секретных файлов:

```bash
cat .gitignore | grep -E "(.env|db\.sqlite3|venv)"
```

Должны быть строки:
- `.env`
- `db.sqlite3`
- `venv/`

Если их нет - они уже добавлены в предыдущих шагах.

## 🎯 Шаг 5: Добавление всех файлов в Git

### 5.1. Проверьте, какие файлы будут добавлены:

```bash
git status
```

Вы увидите список файлов. Убедитесь, что:
- ✅ НЕТ файла `.env` (если есть - создайте его позже локально)
- ✅ НЕТ файла `db.sqlite3` в списке
- ✅ НЕТ папки `venv/` в списке
- ✅ ЕСТЬ все файлы проекта (`.py`, `.md`, `requirements.txt` и т.д.)

### 5.2. Добавьте все файлы:

```bash
git add .
```

### 5.3. Проверьте, что добавилось:

```bash
git status
```

Теперь все файлы должны быть зелеными (staged для commit).

## 💾 Шаг 6: Создание первого коммита

```bash
git commit -m "Initial commit: Калькулятор юнит-экономики Ozon"
```

Если это первый коммит, Git может попросить настроить имя и email (см. Шаг 3).

## 🌐 Шаг 7: Создание репозитория на GitHub

### 7.1. Войдите на GitHub

Откройте https://github.com и войдите в аккаунт.

### 7.2. Создайте новый репозиторий

1. Нажмите кнопку **"+"** в правом верхнем углу
2. Выберите **"New repository"**

### 7.3. Заполните форму создания репозитория:

- **Repository name**: `ozon-calculator` (или любое другое название)
- **Description**: `REST API для расчета прибыли при продаже товаров на маркетплейсе Ozon`
- **Visibility**: Выберите **Public** ⚠️ (это сделает репозиторий публичным!)
- **НЕ отмечайте** "Initialize this repository with a README" (у вас уже есть README)
- **НЕ выбирайте** .gitignore или license (они уже есть)

### 7.4. Нажмите **"Create repository"**

## 🔗 Шаг 8: Связывание локального проекта с GitHub

После создания репозитория GitHub покажет страницу с инструкциями. Найдите раздел **"…or push an existing repository from the command line"**.

Выполните эти команды (GitHub покажет точные команды для вашего репозитория):

```bash
# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/ozon-calculator.git

# Переименуйте главную ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

**Пример для пользователя `ruslanilin`:**

```bash
git remote add origin https://github.com/ruslanilin/ozon-calculator.git
git branch -M main
git push -u origin main
```

### Если попросит авторизацию:

GitHub больше не принимает пароли. Вам нужно:

1. **Создать Personal Access Token:**
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - Дайте название (например, "ozon-calculator")
   - Выберите срок действия
   - Отметьте `repo` (все подпункты)
   - Generate token
   - **СКОПИРУЙТЕ ТОКЕН** (он покажется только один раз!)

2. **При push используйте токен вместо пароля:**
   - Username: ваш GitHub username
   - Password: вставьте токен

## ✅ Шаг 9: Проверка

1. Обновите страницу вашего репозитория на GitHub
2. Вы должны увидеть все файлы проекта
3. README.md должен отображаться на главной странице репозитория

## 🎨 Шаг 10: Настройка репозитория (опционально)

### Добавьте описание репозитория:

1. На странице репозитория нажмите **⚙️ Settings** (справа вверху)
2. В поле **Description** введите: `REST API для расчета прибыли при продаже товаров на маркетплейсе Ozon`
3. Добавьте **Topics** (теги): `django`, `rest-api`, `ozon`, `calculator`, `python`, `ecommerce`
4. Нажмите **Save**

### Добавьте информацию о сайте (если есть):

Если вы опубликуете проект на сервере, добавьте URL в раздел **Website**.

## 🔄 Шаг 11: Обновление кода в будущем

Когда вы внесете изменения в проект и захотите обновить GitHub:

```bash
# 1. Проверьте статус
git status

# 2. Добавьте измененные файлы
git add .

# 3. Создайте коммит
git commit -m "Описание ваших изменений"

# 4. Отправьте на GitHub
git push
```

## 📌 Шаг 12: Публикация на GitHub Pages (опционально)

Если хотите сделать сайт доступным через GitHub Pages:

1. GitHub → Settings → Pages
2. Source: Deploy from a branch
3. Branch: `main` / `/root`
4. Save
5. Через несколько минут сайт будет доступен по адресу: `https://YOUR_USERNAME.github.io/ozon-calculator`

**Примечание**: GitHub Pages подходит для статических сайтов. Для Django нужен другой хостинг.

## ⚠️ Важные напоминания

### ❌ НЕ коммитьте:
- Файл `.env` (с секретными ключами)
- База данных `db.sqlite3`
- Папка `venv/` (виртуальное окружение)
- Файлы `__pycache__/`

### ✅ Всегда коммитьте:
- Код проекта (`.py` файлы)
- `requirements.txt`
- `README.md`
- `LICENSE`
- `.gitignore`

## 🆘 Решение проблем

### Ошибка: "fatal: not a git repository"
```bash
git init
```

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/ozon-calculator.git
```

### Ошибка: "failed to push some refs"
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### Забыли добавить файл в .gitignore
```bash
# Удалите файл из Git (но не с диска)
git rm --cached .env
git rm --cached db.sqlite3
git rm -r --cached venv/

# Обновите .gitignore (если нужно)
# Затем:
git add .gitignore
git commit -m "Update .gitignore"
git push
```

### Нужно удалить коммит с секретными данными
Если случайно закоммитили `.env` или секретный ключ:
1. Сразу же поменяйте SECRET_KEY в `.env`
2. Следуйте инструкциям GitHub: https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository

## 📞 Нужна помощь?

- GitHub Docs: https://docs.github.com
- Git Handbook: https://guides.github.com/introduction/git-handbook/
- Создайте Issue в вашем репозитории для вопросов

---

**Готово!** Ваш проект теперь публично доступен на GitHub! 🎉

