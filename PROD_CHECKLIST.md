# ✅ Чеклист для проверки работы категорий в продакшене

После публикации сервиса убедитесь, что категории работают:

## 🔍 Быстрая проверка

1. **Откройте сайт** в браузере
2. **Кликните на поле "Категория"** - должен появиться список категорий
3. **Начните вводить название** (например, "телефон") - должны появиться результаты поиска
4. **Выберите категорию** - должны показаться комиссии FBO и FBS

## 🛠️ Если категории не загружаются

### Проверка 1: API работает
Откройте в браузере:
```
https://ваш-домен.onrender.com/api/categories/?page_size=10
```

Должен вернуться JSON с категориями. Если пусто - см. Проверка 2.

### Проверка 2: Категории в базе данных
В Render Dashboard → Shell выполните:
```bash
python manage.py shell -c "from categories.models import Category; print(f'Категорий в БД: {Category.objects.count()}')"
```

Если показывает `0`:
```bash
python manage.py load_categories
```

### Проверка 3: Консоль браузера
1. Откройте DevTools (F12)
2. Вкладка Console
3. Проверьте логи - должны быть сообщения:
   - `Загружено X категорий` при загрузке страницы
   - Нет ошибок типа `Failed to fetch` или `CORS error`

### Проверка 4: Сеть (Network)
1. DevTools → Network
2. Перезагрузите страницу
3. Найдите запрос к `/api/categories/`
4. Статус должен быть `200 OK`
5. Response должен содержать `{"results": [...]}`

## 🔧 Настройки для продакшена

Убедитесь что в Render Dashboard → Environment Variables установлено:

```
DEBUG=False
ALLOWED_HOSTS=ваш-домен.onrender.com
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOW_CREDENTIALS=True
```

## 📝 Автоматическая загрузка

Категории загружаются автоматически при первом запуске, если база пустая. 
Логи загрузки видны в Render Dashboard → Logs.

Если нужно загрузить вручную:
```bash
python manage.py load_categories
```

## ⚠️ Частые проблемы

### Проблема: "Категории не найдены"
**Решение**: Проверьте что категории загружены в БД (см. Проверка 2)

### Проблема: CORS ошибка
**Решение**: Убедитесь что `CORS_ALLOW_ALL_ORIGINS=True` в переменных окружения

### Проблема: API возвращает 404
**Решение**: Проверьте что миграции выполнены:
```bash
python manage.py migrate
```

### Проблема: Категории не показываются при клике
**Решение**: Откройте консоль браузера (F12) и проверьте ошибки JavaScript

## ✅ Всё работает?

Если всё проверки пройдены:
1. ✅ Категории показываются при клике на поле поиска
2. ✅ Поиск работает при вводе текста
3. ✅ Выбор категории показывает комиссии
4. ✅ API возвращает данные

Значит всё настроено правильно! 🎉

