# 🚀 Инструкция по размещению проекта в интернете

Это подробное руководство, как сделать ваш проект доступным через браузер для всех пользователей.

## 🌐 Платформы для размещения

### Рекомендуемые бесплатные платформы:

1. **Render.com** ⭐ (рекомендую) - простой, бесплатный план
2. **Railway.app** - современная платформа, бесплатный план
3. **Fly.io** - быстрая, бесплатный план

---

## 📦 Вариант 1: Render.com (Самый простой)

### Шаг 1: Регистрация

1. Откройте https://render.com
2. Зарегистрируйтесь через GitHub (удобнее всего)
3. Подтвердите email

### Шаг 2: Создание Web Service

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий `ozon-calculator`
3. Нажмите **"Connect"**

### Шаг 3: Настройка

**Вариант A (Рекомендуется - с Procfile):**

Заполните форму:

- **Name**: `ozon-calculator` (или любое имя)
- **Environment**: `Python 3`
- **Region**: выберите ближайший (например, Frankfurt)
- **Branch**: `main`
- **Root Directory**: оставьте пустым
- **Build Command**:
  ```bash
  pip install -r requirements.txt && python manage.py migrate --noinput && python manage.py collectstatic --noinput
  ```
  
  Эта команда:
  - Устанавливает зависимости
  - Применяет миграции базы данных
  - Собирает статические файлы

  ⚠️ **ВАЖНО:** Миграции должны выполняться в Build Command, а НЕ в Procfile!
- **Start Command**: 
  
  **ПРОБЛЕМА:** Render иногда не видит Procfile автоматически.
  
  **РЕШЕНИЕ:** Укажите Start Command вручную:
  ```bash
  gunicorn ozon_calculator.wsgi:application --bind 0.0.0.0:$PORT --timeout 120 --workers 2 --log-level info
  ```
  
  ⚠️ **ВАЖНО:** 
  - НЕ используйте `bash gunicorn` - это вызовет ошибку!
  - Команда должна начинаться с `gunicorn`, а не `bash gunicorn`!

### Шаг 4: Переменные окружения

В разделе **Environment Variables** добавьте:

```
SECRET_KEY=сгенерируйте-новый-секретный-ключ
DEBUG=False
ALLOWED_HOSTS=ozon-calculator.onrender.com
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOW_CREDENTIALS=True
```

⚠️ **ВАЖНО**: Для работы категорий на сайте нужно установить `CORS_ALLOW_ALL_ORIGINS=True`, так как фронтенд и API на одном домене.

**Как сгенерировать SECRET_KEY:**

**Способ 1 (если виртуальное окружение активировано):**
```bash
cd "/Users/ruslanilin/Desktop/cursor-prod/ecom-economy/project file"
source venv/bin/activate  # или venv\Scripts\activate на Windows
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

**Способ 2 (с python3 напрямую, если Django установлен глобально):**
```bash
python3 -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

**Способ 3 (онлайн генератор):**
Или используйте онлайн-генератор: https://djecrety.ir/

**Способ 4 (используя скрипт из проекта):**
```bash
cd "/Users/ruslanilin/Desktop/cursor-prod/ecom-economy/project file"
source venv/bin/activate
python generate_secret_key.py
```

**Способ 5 (через Django management команду):**
```bash
source venv/bin/activate
python manage.py shell -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

**Рекомендация:** Используйте **Способ 1** или **Способ 4** - они самые надежные.

Скопируйте сгенерированный ключ и вставьте в `SECRET_KEY` в переменных окружения.

### Шаг 5: Деплой

1. Нажмите **"Create Web Service"**
2. Дождитесь завершения деплоя (5-10 минут)
3. Ваш сайт будет доступен по адресу: `https://ozon-calculator.onrender.com`

### Шаг 6: Загрузка данных

После первого деплоя нужно загрузить категории:

1. Откройте **Shell** в Render Dashboard
2. Выполните:
   ```bash
   python manage.py migrate
   python manage.py load_categories
   ```

---

## 🚂 Вариант 2: Railway.app

### Шаг 1: Регистрация

1. Откройте https://railway.app
2. Зарегистрируйтесь через GitHub

### Шаг 2: Создание проекта

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите репозиторий `ozon-calculator`

### Шаг 3: Настройка

Railway автоматически определит Python проект по `requirements.txt` и `Procfile`.

### Шаг 4: Переменные окружения

В разделе **Variables** добавьте:

```
SECRET_KEY=ваш-секретный-ключ
DEBUG=False
ALLOWED_HOSTS=${{RAILWAY_PUBLIC_DOMAIN}}
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=https://${{RAILWAY_PUBLIC_DOMAIN}}
```

Railway автоматически создаст домен вида `your-project.up.railway.app`

### Шаг 5: Загрузка данных

После деплоя:
1. Откройте **Shell** в Railway
2. Выполните:
   ```bash
   python manage.py migrate
   python manage.py load_categories
   ```

---

## ✈️ Вариант 3: Fly.io

### Шаг 1: Установка Fly CLI

```bash
curl -L https://fly.io/install.sh | sh
```

### Шаг 2: Логин

```bash
flyctl auth login
```

### Шаг 3: Создание приложения

```bash
cd "/Users/ruslanilin/Desktop/cursor-prod/ecom-economy/project file"
flyctl launch
```

Следуйте инструкциям в терминале.

### Шаг 4: Деплой

```bash
flyctl deploy
```

---

## 🔧 Локальная подготовка перед деплоем

### 1. Проверьте, что все файлы добавлены в Git:

```bash
git status
```

Должны быть видны:
- `Procfile` ✅
- `runtime.txt` ✅
- `requirements.txt` ✅

### 2. Закоммитьте изменения:

```bash
git add Procfile runtime.txt requirements.txt ozon_calculator/settings.py
git commit -m "Add deployment configuration"
git push
```

---

## 🔍 Проверка после деплоя

После успешного деплоя проверьте:

1. **Главная страница**: `https://ваш-домен.com/`
2. **API Документация**: `https://ваш-домен.com/api/docs/`
3. **API эндпоинты**: `https://ваш-домен.com/api/categories/`

---

## 🐛 Решение проблем

### Ошибка: "No module named 'gunicorn'"

Убедитесь, что `gunicorn` в `requirements.txt`.

### Ошибка: Static files не загружаются

Проверьте:
1. Выполнен ли `collectstatic`
2. Настроен ли `STATIC_ROOT` в settings.py
3. Настроен ли WhiteNoise

### Ошибка: "DisallowedHost"

Проверьте `ALLOWED_HOSTS` в переменных окружения:
```bash
ALLOWED_HOSTS=ваш-домен.com,www.ваш-домен.com
```

### Ошибка: Database migrations

Выполните в Shell платформы:
```bash
python manage.py migrate
```

### Ошибка: Категории не загружены

Выполните:
```bash
python manage.py load_categories
```

---

## 💰 Стоимость

Все упомянутые платформы имеют **бесплатные планы**, достаточные для начала:

- **Render**: бесплатный план с ограничениями (может "засыпать" при бездействии)
- **Railway**: $5 бесплатных кредитов в месяц
- **Fly.io**: бесплатный план с ограничениями

Для коммерческого использования рекомендуется платный план.

---

## 📝 Что дальше?

После деплоя:

1. ✅ Добавьте ссылку на сайт в README.md на GitHub
2. ✅ Настройте кастомный домен (опционально)
3. ✅ Включите мониторинг (опционально)
4. ✅ Настройте автоматические деплои при push в GitHub

---

## 🆘 Нужна помощь?

- Render Docs: https://render.com/docs
- Railway Docs: https://docs.railway.app
- Fly.io Docs: https://fly.io/docs

Удачи с деплоем! 🚀

