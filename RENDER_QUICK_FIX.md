# 🚀 Быстрое исправление "Application exited early" на Render

## ⚡ Экстренные действия (Сделайте прямо сейчас)

### 1. Проверьте переменные окружения в Render

**Обязательно должны быть установлены:**

1. Откройте Render Dashboard → ваш сервис → **Settings** → **Environment**
2. Убедитесь, что есть:

```
SECRET_KEY=ваш-ключ-здесь
DEBUG=False
ALLOWED_HOSTS=ozon-calculator-1.onrender.com
```

⚠️ **КРИТИЧНО:** Замените `ozon-calculator-1.onrender.com` на ваш реальный домен из Render!

**Если SECRET_KEY нет:**
1. Сгенерируйте: `python generate_secret_key.py`
2. Скопируйте ключ
3. Добавьте в Render как переменную окружения

### 2. Обновите Build Command

В **Settings** → **Build & Deploy** → **Build Command** должно быть:

```bash
pip install -r requirements.txt && python manage.py migrate --noinput && python manage.py collectstatic --noinput
```

### 3. Убедитесь, что Start Command ПУСТОЕ

В **Settings** → **Build & Deploy** → **Start Command**:
- Должно быть **ПУСТОЕ** поле!
- Render использует Procfile автоматически

### 4. Закоммитьте изменения

```bash
cd "/Users/ruslanilin/Desktop/cursor-prod/ecom-economy/project file"
git add Procfile ozon_calculator/settings.py
git commit -m "Fix deployment settings"
git push
```

### 5. Подождите перезапуска

Render автоматически перезапустит деплой. Проверьте логи через 2-3 минуты.

---

## 🔍 Если не помогло - проверьте логи

1. Откройте вкладку **Logs** в Render
2. Найдите последние строки перед "Application exited early"
3. Ищите ошибки (обычно красные или содержат "Error", "Failed", "Exception")

**Типичные ошибки:**

- `ImproperlyConfigured: The SECRET_KEY setting must not be empty` → SECRET_KEY не установлен
- `DisallowedHost` → ALLOWED_HOSTS не содержит ваш домен
- `ModuleNotFoundError` → зависимость не установлена
- `OperationalError` → проблема с базой данных

---

## 💡 Альтернатива: Временно упростите настройки

Если проблема сохраняется, временно отключите строгие настройки безопасности:

В Render → Environment Variables добавьте:
```
DEBUG=True
```

Это включит режим отладки и покажет детальные ошибки в логах.

⚠️ **Не забудьте вернуть `DEBUG=False`** после исправления!

---

## 📞 Что дальше?

После выполнения этих шагов:
1. Подождите 3-5 минут
2. Проверьте логи
3. Попробуйте открыть сайт: `https://ozon-calculator-1.onrender.com`

Если всё работает - отлично! Если нет - пришлите последние 20-30 строк из логов.

