# Django Settings
# ВНИМАНИЕ: Создайте файл .env и установите свои значения
# НЕ коммитьте файл .env в репозиторий!

# Django Secret Key (обязательно для продакшена)
# Сгенерируйте новый ключ командой:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-change-this-in-production

# Debug режим (False для продакшена)
DEBUG=True

# Разрешенные хосты (разделить запятыми)
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (опционально, по умолчанию используется SQLite)
# Для PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# CORS настройки
# Для разработки:
CORS_ALLOW_ALL_ORIGINS=True

# Для продакшена укажите конкретные домены:
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# CORS_ALLOW_CREDENTIALS=True
