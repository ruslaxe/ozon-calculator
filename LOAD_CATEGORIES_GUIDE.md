# 📦 Загрузка категорий на Render

## ✅ Сайт работает, но категории не загружены

После успешного деплоя нужно загрузить категории товаров в базу данных.

## 🔧 Способ 1: Через Shell в Render (Самый простой)

### Шаг 1: Откройте Shell

1. Render Dashboard → ваш сервис `озон-калькулятор-1`
2. В левом меню нажмите **"Shell"**
3. Откроется терминал

### Шаг 2: Проверьте количество категорий

```bash
python manage.py shell -c "from categories.models import Category; print(Category.objects.count())"
```

Если показывает `0` - категории не загружены.

### Шаг 3: Загрузите категории

**Просто выполните команду:**

```bash
python manage.py load_categories
```

Эта команда:
- Попытается загрузить категории из Excel файла, если он доступен
- Если Excel файла нет - создаст 10 тестовых категорий для работы

**Команда создаст тестовые категории:**
- Шарф, 3D-очки, Футболка, Книга, Наушники, Смартфон, Ноутбук, Кроссовки, Часы, Рюкзак

---

## 🔧 Способ 2: Загрузка из Excel файла

Если у вас есть Excel файл с категориями Ozon:

1. Загрузите файл на Render (через Shell или через другую платформу)
2. Выполните:
   ```bash
   python manage.py import_ozon_categories путь/к/файлу.xlsx
   ```

---

## 📋 После загрузки

1. Проверьте API: `https://ozon-calculator-1.onrender.com/api/categories/`
2. Должен вернуться список категорий (не пустой!)
3. Проверьте поиск на главной странице

---

## ⚠️ Важно

Для полноценной работы нужны реальные категории Ozon (около 15,000).
Текущее решение - временное для тестирования.

---

## 💡 Решение для автоматической загрузки

Для автоматической загрузки категорий при деплое нужно:
1. Создать команду `load_categories` которая загружает данные из файла в репозитории
2. Добавить команду в Build Command в Render

Хотите, чтобы я создал такую команду?

