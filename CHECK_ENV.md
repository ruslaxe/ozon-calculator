# ✅ Проверка переменных окружения на Render

## 🔍 Обязательные переменные окружения

Убедитесь, что в Render Dashboard → Settings → Environment установлены **все** эти переменные:

### 1. SECRET_KEY (ОБЯЗАТЕЛЬНО!)
```
SECRET_KEY=ваш-сгенерированный-ключ-здесь
```
**Как получить:** Запустите `python generate_secret_key.py` или используйте https://djecrety.ir/

### 2. DEBUG
```
DEBUG=False
```

### 3. ALLOWED_HOSTS
```
ALLOWED_HOSTS=ozon-calculator-1.onrender.com
```
⚠️ Замените на ваш реальный домен из Render!

### 4. CORS (Опционально, но рекомендуется)
```
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=https://ozon-calculator-1.onrender.com
```

---

## 🔍 Как проверить в Render

1. Откройте ваш сервис в Render Dashboard
2. Перейдите в **Settings** → **Environment**
3. Убедитесь, что все переменные установлены
4. Если какой-то переменной нет - добавьте её, нажав **"Add Environment Variable"**

---

## ⚠️ Частые ошибки

### Ошибка: "ImproperlyConfigured: The SECRET_KEY setting must not be empty"

**Причина:** SECRET_KEY не установлен в переменных окружения

**Решение:** 
1. Сгенерируйте новый SECRET_KEY
2. Добавьте его в переменные окружения в Render
3. Перезапустите деплой

---

### Ошибка: "DisallowedHost at /"

**Причина:** ALLOWED_HOSTS не содержит ваш домен Render

**Решение:**
1. Проверьте ваш домен в Render (например: `ozon-calculator-1.onrender.com`)
2. Убедитесь, что в ALLOWED_HOSTS указан этот домен
3. Не используйте `*` в продакшене

---

## 📋 Минимальный набор переменных для запуска

Если приложение все равно не запускается, используйте минимальный набор:

```
SECRET_KEY=сгенерированный-ключ
DEBUG=False
ALLOWED_HOSTS=ozon-calculator-1.onrender.com
```

Остальные переменные можно добавить позже.

---

## 🔧 Проверка через Shell

После деплоя можно проверить переменные через Shell в Render:

1. Откройте **Shell** в Render Dashboard
2. Выполните:
   ```bash
   echo $SECRET_KEY
   echo $DEBUG
   echo $ALLOWED_HOSTS
   ```

Если переменные пустые - они не установлены правильно.

