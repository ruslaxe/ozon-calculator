# 🔄 Автоматическая загрузка категорий

## ✅ Проблема решена!

На бесплатном плане Render нет доступа к Shell, поэтому категории теперь загружаются **автоматически** при первом запуске приложения.

## 🔧 Как это работает

1. При запуске приложения проверяется, есть ли категории в базе данных
2. Если база пустая - автоматически загружаются категории из Excel файла
3. Если Excel файл не найден - создаются тестовые категории

## 📋 Что было сделано

- ✅ Создан `ozon_calculator/startup.py` - модуль для автоматической загрузки
- ✅ Обновлен `ozon_calculator/wsgi.py` - вызывает загрузку при старте
- ✅ Обновлен `Procfile` - добавляет миграции перед запуском

## ⏱️ Когда загружаются категории

Категории загружаются:
- При первом запуске приложения (если база пустая)
- Автоматически при каждом деплое (если категорий нет)
- **НЕ загружаются повторно**, если они уже есть в базе

## 🔍 Проверка

После следующего деплоя:

1. Откройте логи в Render Dashboard → **Logs**
2. Ищите строки:
   ```
   📦 База данных пустая. Загружаю категории...
   📁 Найден Excel файл: ...
   📥 Загружаю категории из Excel файла...
   ✅ Категории успешно загружены! Всего: 15000+
   ```

3. Проверьте API: `https://ozon-calculator-1.onrender.com/api/categories/`
4. Должен вернуться список категорий!

## ⚠️ Первый запуск

При первом запуске загрузка категорий может занять 2-5 минут (15,000+ записей).
Приложение будет доступно сразу, но категории появятся через несколько минут.

## 💡 Если категории не загрузились

1. Проверьте логи в Render - там будет ошибка
2. Убедитесь, что Excel файл есть в репозитории в папке `docs/`
3. Если проблема сохраняется - категории можно загрузить вручную при обновлении до платного плана

---

**Готово!** Теперь категории загружаются автоматически, без необходимости доступа к Shell! 🎉

