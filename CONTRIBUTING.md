# Руководство по внесению вклада

Спасибо за интерес к проекту! Мы рады любому вкладу.

## Как внести вклад

### Сообщить об ошибке

Если вы нашли ошибку:
1. Убедитесь, что ошибка еще не была зарегистрирована в Issues
2. Создайте новый Issue с подробным описанием:
   - Что произошло
   - Как воспроизвести проблему
   - Ожидаемое поведение
   - Фактическое поведение
   - Версия Python, Django и другие релевантные детали

### Предложить новую функцию

1. Создайте Issue с описанием предлагаемой функции
2. Обсудите реализацию перед началом работы
3. Дождитесь одобрения перед созданием Pull Request

### Отправить Pull Request

1. **Fork репозитория**
   ```bash
   git clone <your-fork-url>
   cd ozon-calculator
   ```

2. **Создайте ветку для вашей функции**
   ```bash
   git checkout -b feature/your-feature-name
   ```

3. **Настройте окружение разработки**
   ```bash
   python -m venv venv
   source venv/bin/activate  # или venv\Scripts\activate на Windows
   pip install -r requirements.txt
   python manage.py migrate
   ```

4. **Внесите изменения**
   - Следуйте стилю кода проекта
   - Добавляйте комментарии где необходимо
   - Обновляйте документацию при изменении API

5. **Проверьте работу**
   - Убедитесь, что сервер запускается
   - Проверьте работу новых функций
   - Проверьте, что существующие тесты проходят (если есть)

6. **Сделайте commit**
   ```bash
   git add .
   git commit -m "Описание ваших изменений"
   ```

7. **Отправьте изменения**
   ```bash
   git push origin feature/your-feature-name
   ```

8. **Создайте Pull Request**
   - Опишите, что было изменено
   - Укажите связанные Issues (если есть)
   - Добавьте скриншоты для изменений UI

## Стандарты кода

### Python

- Следуйте PEP 8
- Используйте понятные имена переменных и функций
- Добавляйте docstrings для функций и классов
- Комментируйте сложную логику

### Django/DRF

- Используйте стандартные паттерны Django
- Разделяйте логику: models, serializers, views, services
- Используйте serializers для валидации
- Обрабатывайте ошибки корректно

### Git

- Используйте понятные сообщения commit
- Один commit = одна логическая единица изменения
- Не коммитьте файлы `.env`, `db.sqlite3`, `__pycache__/`

## Структура проекта

```
ozon_calculator/        # Главные настройки Django
calculator/             # Приложение калькулятора
categories/             # Приложение категорий
frontend/               # Веб-интерфейс (HTML/CSS/JS)
docs/                   # Документация
```

## Вопросы?

Создайте Issue с вопросом, и мы постараемся помочь!

---

Спасибо за ваш вклад! 🎉

