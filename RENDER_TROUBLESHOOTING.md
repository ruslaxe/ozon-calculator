# 🔧 Исправление ошибки "Application exited early" на Render

## ❌ Проблема

Приложение завершается сразу после запуска с ошибкой:
```
Application exited early while running your code
```

## 🔍 Возможные причины

1. **Не применены миграции базы данных**
2. **Отсутствуют или неверны переменные окружения**
3. **Ошибка при запуске gunicorn**
4. **Проблемы с SECRET_KEY**

## ✅ Решение

### Шаг 1: Обновите Build Command в Render

В настройках вашего сервиса на Render:

1. Откройте **Settings** → **Build & Deploy**
2. Найдите раздел **Build Command**
3. Убедитесь, что указано:
   ```bash
   pip install -r requirements.txt && python manage.py migrate --noinput && python manage.py collectstatic --noinput
   ```
4. Сохраните изменения

### Шаг 2: Проверьте Start Command

1. В **Settings** → **Build & Deploy**
2. Поле **Start Command** должно быть **ПУСТЫМ**
3. Render автоматически использует Procfile

### Шаг 3: Проверьте переменные окружения

В **Settings** → **Environment** убедитесь, что установлены:

```
SECRET_KEY=ваш-секретный-ключ-здесь
DEBUG=False
ALLOWED_HOSTS=ozon-calculator-1.onrender.com
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=https://ozon-calculator-1.onrender.com
```

**Важно:** 
- Замените `ozon-calculator-1.onrender.com` на ваш реальный домен из Render
- SECRET_KEY должен быть установлен!

### Шаг 4: Проверьте логи

1. Откройте вкладку **Logs** в Render Dashboard
2. Прокрутите до самого начала логов
3. Ищите ошибки (они обычно красные)

### Шаг 5: Обновите Procfile в проекте

Убедитесь, что `Procfile` содержит:
```
web: python manage.py migrate --noinput && gunicorn ozon_calculator.wsgi:application --bind 0.0.0.0:$PORT
```

Если Procfile был обновлен:
```bash
cd "/Users/ruslanilin/Desktop/cursor-prod/ecom-economy/project file"
git add Procfile
git commit -m "Fix Procfile: add migrations before start"
git push
```

### Шаг 6: Ручное выполнение миграций (если нужно)

Если автоматические миграции не работают:

1. В Render Dashboard откройте **Shell**
2. Выполните:
   ```bash
   python manage.py migrate
   python manage.py load_categories
   ```

---

## 🔍 Проверка всех настроек

### ✅ Checklist перед деплоем:

- [ ] `Procfile` существует и правильный
- [ ] `requirements.txt` содержит все зависимости
- [ ] `runtime.txt` указывает версию Python (например, `python-3.11.9`)
- [ ] `SECRET_KEY` установлен в переменных окружения
- [ ] `DEBUG=False` установлен
- [ ] `ALLOWED_HOSTS` содержит ваш домен Render
- [ ] Build Command включает миграции
- [ ] Start Command **ПУСТОЕ** (используется Procfile)

---

## 🆘 Дополнительная диагностика

### Проверьте логи в Render:

Откройте **Logs** и найдите последние строки перед ошибкой. Типичные ошибки:

1. **"ModuleNotFoundError"** - зависимость не установлена → проверьте `requirements.txt`
2. **"django.core.exceptions.ImproperlyConfigured"** - проблема с настройками → проверьте переменные окружения
3. **"Database locked"** - проблема с SQLite → миграции должны выполняться автоматически
4. **"No such table"** - миграции не применены → добавьте в Build Command

---

## 💡 Альтернативное решение

Если проблема сохраняется, создайте новый сервис на Render с нуля:

1. Удалите старый сервис
2. Создайте новый **Web Service**
3. Подключите тот же репозиторий
4. Используйте обновленные настройки из `DEPLOYMENT_GUIDE.md`
5. Убедитесь, что все переменные окружения установлены

---

Если проблема сохраняется, пришлите полные логи из раздела **Logs** в Render.

