# ⚡ Быстрый старт: Публикация проекта

## ✅ Шаг 1: Установка Git (если еще не установлен)

В терминале выполните:

```bash
xcode-select --install
```

**Что произойдет:**
1. Появится диалоговое окно macOS
2. Нажмите **"Установить"**
3. Дождитесь завершения (5-15 минут)
4. Перезапустите терминал

**Проверьте установку:**
```bash
git --version
```

Должно показать что-то вроде: `git version 2.x.x`

---

## ✅ Шаг 2: Переход в папку проекта

```bash
cd "/Users/ruslanilin/Desktop/cursor-prod/ecom-economy/project file"
```

---

## ✅ Шаг 3: Инициализация Git (если еще не сделано)

```bash
git init
```

---

## ✅ Шаг 4: Настройка Git (первый раз)

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@gmail.com"
```

**Важно:** Используйте email, который привязан к вашему GitHub аккаунту!

---

## ✅ Шаг 5: Добавление файлов

```bash
git add .
```

---

## ✅ Шаг 6: Создание первого коммита

```bash
git commit -m "Initial commit: Калькулятор юнит-экономики Ozon"
```

---

## ✅ Шаг 7: Создание репозитория на GitHub

1. Откройте https://github.com и войдите
2. Нажмите **"+"** → **"New repository"**
3. Название: `ozon-calculator`
4. Описание: `REST API для расчета прибыли при продаже товаров на Ozon`
5. Выберите **Public** ⚠️
6. **НЕ** отмечайте README, .gitignore, license (они уже есть!)
7. Нажмите **"Create repository"**

---

## ✅ Шаг 8: Получение токена доступа GitHub

GitHub требует токен вместо пароля:

1. GitHub → **Settings** (правый верхний угол)
2. Внизу слева: **Developer settings**
3. **Personal access tokens** → **Tokens (classic)**
4. **Generate new token (classic)**
5. Название: `ozon-calculator`
6. Срок: выберите нужный (например, 90 дней)
7. Отметьте **`repo`** (все галочки под ним)
8. Нажмите **Generate token** внизу
9. **СКОПИРУЙТЕ ТОКЕН** (он показывается только один раз!) ✋

---

## ✅ Шаг 9: Подключение к GitHub и отправка кода

**Замените `YOUR_USERNAME` на ваш GitHub username:**

```bash
# Добавьте удаленный репозиторий
git remote add origin https://github.com/YOUR_USERNAME/ozon-calculator.git

# Переименуйте ветку в main
git branch -M main

# Отправьте код
git push -u origin main
```

**При запросе авторизации:**
- Username: ваш GitHub username
- Password: **вставьте токен** (не пароль!)

---

## ✅ Готово!

Откройте ваш репозиторий на GitHub:
`https://github.com/YOUR_USERNAME/ozon-calculator`

---

## 🆘 Проблемы?

### Ошибка: "fatal: not a git repository"
```bash
git init
```

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/ozon-calculator.git
```

### Ошибка при push: "authentication failed"
- Проверьте, что используете **токен**, а не пароль
- Убедитесь, что токен имеет права `repo`

### Нужна помощь?
Смотрите подробную инструкцию: [PUBLISHING_GUIDE.md](PUBLISHING_GUIDE.md)

