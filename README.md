# Калькулятор юнит-экономики Ozon

REST API и веб-приложение для расчета прибыли при продаже товаров на маркетплейсе Ozon. Поддерживает расчеты для схем FBO (Fulfillment by Ozon) и FBS (Fulfillment by Seller) с учетом всех комиссий и затрат.

## 🚀 Возможности

- 📊 Расчет прибыли для товаров на маркетплейсе Ozon
- 🔍 Поиск по категориям товаров (более 15,000 категорий)
- 📦 Поддержка схем доставки FBO и FBS
- 💰 Учет всех затрат: комиссии, эквайринг, обработка, доставка, налоги
- 📐 Два режима ввода: габариты (Д×Ш×В) или объём
- 📈 Визуальные индикаторы прибыльности
- 📄 Экспорт расчетов в Excel
- 📚 Полная API документация (Swagger/ReDoc)

## 📋 Требования

- Python 3.8+
- Django 4.2+
- SQLite (по умолчанию) или PostgreSQL (для продакшена)

## 🛠️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd ozon-calculator
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
```

### 3. Активация виртуального окружения

**Windows:**
```bash
venv\Scripts\activate
```

**macOS/Linux:**
```bash
source venv/bin/activate
```

### 4. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 5. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и установите необходимые значения (см. раздел "Конфигурация" ниже).

### 6. Применение миграций

```bash
python manage.py migrate
```

### 7. Создание суперпользователя (опционально, для доступа к админке)

```bash
python manage.py createsuperuser
```

### 8. Загрузка начальных данных (категории)

```bash
python manage.py load_categories
```

Эта команда загрузит более 15,000 категорий товаров Ozon с актуальными комиссиями.

### 9. Запуск сервера разработки

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: `http://127.0.0.1:8000/`

## 🌐 Веб-интерфейс

После запуска сервера откройте в браузере:
- **Главная страница (калькулятор)**: `http://127.0.0.1:8000/`
- **API Документация (Swagger)**: `http://127.0.0.1:8000/api/docs/`
- **API Документация (ReDoc)**: `http://127.0.0.1:8000/api/redoc/`
- **Django Admin**: `http://127.0.0.1:8000/admin/` (требует создания суперпользователя)

### Возможности веб-интерфейса:
- ✅ Поиск и выбор категории товара
- ✅ Отображение комиссий FBO и FBS
- ✅ Два режима ввода: Габариты / Объём
- ✅ Автоматический расчёт объёма из габаритов
- ✅ Настройка параметров расчёта (налог, выкуп, доставка)
- ✅ Таблица с детальными результатами для FBO и FBS
- ✅ Визуальные индикаторы прогресса
- ✅ Кнопка "Пересчитать" и "Сбросить настройки"
- ✅ Экспорт результатов в Excel

## 📡 API эндпоинты

### Категории

- `GET /api/categories/` - Поиск категорий товаров
  - Параметры: `search` (поиск по названию), `category_group` (фильтр по группе)
  - Пагинация: 20 результатов на страницу

- `GET /api/categories/{id}/` - Получение конкретной категории

### Калькулятор

- `POST /api/calculate/` - Расчет прибыли
  - Принимает данные о товаре (цена, вес, объём/габариты)
  - Возвращает результаты для FBO и FBS схем

Подробные примеры запросов см. в файле [API_EXAMPLES.md](API_EXAMPLES.md)

## ⚙️ Конфигурация

Проект использует переменные окружения для настройки. Создайте файл `.env` в корне проекта:

```env
# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (опционально, по умолчанию используется SQLite)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# CORS (для продакшена укажите конкретные домены)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000
```

Для генерации безопасного SECRET_KEY используйте:
```python
from django.core.management.utils import get_random_secret_key
print(get_random_secret_key())
```

## 📚 Документация

- [Примеры использования API](API_EXAMPLES.md)
- [Руководство по фронтенду](FRONTEND_GUIDE.md)
- [Дорожная карта проекта](docs/roadmap.md)
- [📤 Инструкция по публикации на GitHub](PUBLISHING_GUIDE.md) — пошаговое руководство для новичков
- [🚀 Инструкция по размещению в интернете](DEPLOYMENT_GUIDE.md) — как сделать проект доступным через браузер

## 🔒 Безопасность

**Важно для продакшена:**
- Установите `DEBUG=False` в `.env`
- Используйте надежный `SECRET_KEY`
- Настройте `ALLOWED_HOSTS` с конкретными доменами
- Настройте `CORS_ALLOWED_ORIGINS` вместо `CORS_ALLOW_ALL_ORIGINS`
- Используйте HTTPS
- Настройте rate limiting для API

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста:
1. Сделайте fork репозитория
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Сделайте commit изменений (`git commit -m 'Add amazing feature'`)
4. Отправьте изменения в ветку (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. Подробности см. в файле [LICENSE](LICENSE).

## 📞 Контакты

Если у вас есть вопросы или предложения, создайте Issue в репозитории.

---

**Примечание:** Комиссии и тарифы Ozon могут изменяться. Проект использует актуальные данные на момент загрузки категорий. Рекомендуется периодически обновлять данные категорий.

